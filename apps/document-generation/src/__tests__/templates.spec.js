import { getAllTemplates } from "../templates";

describe("templates #getAllTemplates", () => {
  it("throws error if disputeType doesn't match any template source", () => {});
  it("retrieves the set of templates depending on disputeType", () => {});
});
